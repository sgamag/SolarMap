/** Fixed top-of-page banner shown on every page. */
export default function PrototypeBanner() {
  return (
    <div className="bg-primary text-primary-foreground text-xs sm:text-sm py-2 px-4 text-center font-medium">
      Prototipo visual · Datos pendientes de conexión · Universidad Europea de Madrid
    </div>
  );
}
